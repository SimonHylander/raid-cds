import React, {useState} from 'react';
import {ReportLoader} from '../components/Report'

export const Home = () => {
  return (
    <div>
      <ReportLoader/>
    </div>
  );
}

export default Home;
